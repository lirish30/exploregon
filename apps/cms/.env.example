# Core
NODE_ENV=development
# Leave both blank to use local SQLite file (apps/cms/payload.db)
DATABASE_URI=
DATABASE_URL=
PAYLOAD_SECRET=replace-with-a-long-random-secret

# CMS URLs
# Leave blank to auto-use PAYLOAD_PORT (defaults to 3001)
PAYLOAD_PUBLIC_SERVER_URL=
PAYLOAD_PUBLIC_ADMIN_URL=
